
  <%= javascript_include_tag "prototype" %>

<h1>My Tasks</h1>
 <%= select :task, :id,  Task.select("DISTINCT status").find(:all).collect {|p| [ p.status, p.status ] }, {:prompt => 'Select'}, :onchange => "#{remote_function(:url => {:controller =>"tasks", :action => "status"}, :with => "'status='+value",:loading => "Element.show('loader');",:complete => "Element.hide('loader');")}" %>
 

<br>
<br><br>

 <div id ="status">
  <%= render "task_by_status" %>

 </div>


<br>
<br><br>

<%= link_to 'New task', new_task_path %>